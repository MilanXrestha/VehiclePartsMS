namespace VehiclePartsMS.Domain.Models;

public enum UserRole { Admin, Staff, Customer }
public enum PaymentStatus { Paid, Credit }
public enum AppointmentStatus { Pending, Confirmed, Completed, Cancelled }
public enum PartRequestStatus { Pending, Available, Unavailable }
